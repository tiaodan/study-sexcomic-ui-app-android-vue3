# v0.0.0.0
- 使用离线版vue3+element-plus创建项目，创建简单演示index文件
# v0.0.0.1
- 用官方table例子, 显示表格。table.html
# v0.0.0.2
- 遇到跨域错误, 用vs code插件tomcat本地运行
# v0.0.0.3
- 结合vue3+element-plus, 实现简单界面
# v0.0.0.4
- Order表增、改、查已完成
# v0.0.0.4 2025.4.16
- Order表新增，增完重置变量
# v0.0.0.5 2025.4.16v2
- 加上删除，不成功，需要调试

# v0.0.0.5 2025.4.21 
- 需要修改
- 新增完，不更新，需要定位
- 前端很多是共享变量，都放到shared.js里面。如快递公司、颜色等
- 把一些公用变量放在一起，如快递公司、颜色等。update页面 + 显示界面
- 去除没调用的变量
- 使用父组件传的对象时，是否需要深拷贝？如果用的话，怎么用？ - 需要

# v0.0.0.6 2025.4.22
- 实现增、删、改、查
- 组件间通信, 父传子, 深拷贝

# v0.0.0.7
- 配置服务器，可用域名访问，修改访问端口=80
- 解决问题: shared.js 请求，不加类型判断，报错：index-BPopdcVd.js:13 TypeError: n.reduce is not a function

# v0.0.0.8
- 请求表格数据, 显示加载动画

# v0.0.0.9
- 实现分页查询，后续考虑根据筛选条件实现分页

# v0.0.0.9 - 2025.4.26
- 分页查询，显示分页按钮, 改中文有些问题, 未解决

# v0.0.0.10
- 添加、修改, 订单状态加上: 已取消

# v0.0.0.11
- 适配apk，能装了，能请求。因为安装默认都是https请求
- 需要思考: axios请求，是否都要带https:/www.yuming.com,有没有更好的方式

-------------- 从这里都是拷贝过来的内容 本项目从下面开始-----------------
# v1.0.0.0
- 能成功装apk,但是会遮挡电量状态栏，背景改不成透明

# v1.0.0.1
- 解决状态栏文字颜色，安装capytitor statusbar模块就行

# v1.0.0.2
- 修改excel甘特图-敏捷甘特图

# v1.0.0.3
- android 

# v1.0.0.4
解决了以下问题：
2025.7.2 解决问题：
- App.vue设置了background-color, 漫画浏览页，没设置background, 为什么能看到footbar
- 浏览图片，遮挡状态栏 - StatusBar.hide()最有用
- 每个图片无缝衔接，没有空隙  .comic-image 样式 line-height: 0 最管用
- 不能显示底部footbar 漫画页设置background 只要不透明就行了
- 点击图片，显示返回框 需要设置 z-index 超过图片，否则显示不出来
2025.7.3 解决问题：
- 书架，请求书籍数据，然后再渲染
- 漫画图片，请求章节所有图片，然后再渲染
- 漫画浏览界面，点击整个div任何内容，都可以显示statusbar,因为如果没有图片，不显示statusbar
- 'https://www.j88d.com/test/test/shelf' from origin 'http://localhost' 解决调试时cros 问题 (原因链接写的多个test,服务器用了json-server 默认返回头里就是允许跨域了)
- 'https://www.j88d.com/test/ad' from origin 'http://localhost' 解决调试时cros 问题 (原因 ad 图片写在本地缓存了，F12浏览器就是读取不出来，不知道为啥)
- 解决F12 本地调试时，第一次下载后显示广告，后面用缓存后不显示广告 （因为F12 -> 网络 -> 停用缓存，要把这个关闭 -> 但是不解决实际问题 ）
- grid 书籍列表，弄出组件复用方式
- 解决web测试时，报statusBar的错。-> 通过判断是不是web环境 -》if (Capacitor.getPlatform() !== 'web')  实现
- 解决 退出漫画浏览页，电量栏看不清问题，可能配色有问题 -> 通过只控制状态栏 显示/隐藏实现
- 排行榜-左右布局，不影响footbar + tab 标签页，左侧固定，用flex实现最好，最通用方法
- 处理搜索界面，逻辑
2025.7.4 解决问题：
- 处理排行榜界面逻辑， 先把list做成一个通用组件
- 分类，对齐、显示grid (用van-tab 传5个参数，没有的用'' 补齐)

2025.7.7 解决问题：
- 推荐、书架，列表数据多的时候，不能影响footbar->tabbar，表头要固定位置 -》 不用设置全局样式overflow: hidden;，只要子组件设置了heigh:85vh + overflow-y:auto 就能固定组件，并且滚动
- 推荐、书架，列表数据多的时候，不能影响tab 标签页，表头要固定位置  -》 不用设置全局样式overflow: hidden;，只要子组件设置了heigh:85vh + overflow-y:auto 就能固定组件，并且滚动
- 搜索框，避免请求空内容，返回所有，因为是json-server，自己写后台时注意 https://www.jdjd.com/test/shelfComicList?name= -> 参数校验
- 设置全局属性后，网页版滚动条还有，安卓滚动条没了？咋弄？ -> 因为悬浮div，遮挡住了底部home横线，导致出来了滚动条

# v1.0.0.5
- 所有功能，搭配json-server测试，都能实现
- 漫画查看页面，可以隐藏底部手势导航条
- 章节显示，没做

# v1.0.0.6
主要内容：
- 解决：下拉界面，会回弹问题

- 修改App.vue 界面，不用flex布局，用写死高度。因为flex布局，如果内部写了padding，会导致回弹效果
- 解决问题：父div不设置overflow，子div van-tab固定，tab内容滚动 -》通过设置 父div 固定高度，然后设置子div tab内容 height:100%，overflow-y:auto
- 解决书名过长, 3列变2列问题。思路：控制max-width。 max-width: calc(100% / var(--grid-column-num));
- 未根本解决chapterview 隐藏滚动条问题
  - 问题：全局css 设置了隐藏滚动条
  - 现象：参考试验结果
  - 解决：滚动那个div设置了 100vh-1px 生效。100vh 还是会有滚动条
  -试验 ：
    - 说明：ChapterView.vue 有2个div。大div-全屏,没设高度。小div-滚动,设高度。小div包裹 image-list
    - 以下是大div设置高度：
    - 90vh       -> 不显示滚动条
    - 95vh       -> 不显示滚动条
    - 100vh      ->   显示滚动条
    - 100dvh     ->   显示滚动条

    - 有大div,没有小div,大div高度 = 100vh -1px -> 不显示滚动条，但底部有1px空白
    - 有大div,  有小div,小div高度 = 100vh -1px -> 不显示滚动条，底部没有1px空白  -》建议用它
    - 有大div,  有小div,小div高度 = 100vh      -> 显示滚动条
- 调整目录结构。主框架-》各种项目目录，如comic、audiobook -> footbar对应目录 -> 具体界面实现
- 全局css 控制 z-index:1000 悬浮的界面，滚动条隐藏
- 把代码里写死的 px 都改成变量。在全局css里写自定义变量 --xxx
-------- 压缩node_moudles、android 文件夹后 再上传github  ---------

近期待办：

- 解决，漫画浏览页面，右侧有浏览器的滚动条，怎么去掉 -》 不好实现
- 如何在线升级
- 未登录用户，有种方案，请求1/4章节，已登录用户，用token请求数据
- 后台统计哪个请求最多，每天、每周、每月有多少请求，哪个漫画最火
- 忘记密码界面，用户输入校验码，才能改
- ip 被封，的备用解决方案
- 是否要远程上报bug
- 写，搭建一个项目，流程图，用于直观看
- 优化代码，记忆，理解，整理归纳
- 考虑弄一个普通漫画app，比如道诡异仙，这种普通comic
- 分类，用grid实现，少于5个，会显示出灰色背景不好看
- 分类，用grid实现，不能显示下划线，自定义实现很麻烦
- 分类，用van-tab实现，对不齐
- 是否要开后门

-- 阶段
1 部署开发环境 √
2 写vant前端，服务用json-server代替(编写测试数据) √
3 测试android前端，都能请求返回，显示正确 √
4 熟悉、理解、归纳、掌握前端代码
5 写后端，实现真实请求
6 修改前端,用真实请求,都能请求返回，显示正确
7 测试android前端，都能请求返回，显示正确
8 后端爬取真实数据，传到aws+百度云
9 集成测试
10 发布


-  -------------------注册输入框限制位数



待办:
- 防盗处理，前端、后端 （考虑根据env 分版本打包，测试版+生产版，app有问题上报服务器，用于自己定位）
- 真实打包apk前，把vue源代码+混淆代码，不关联（即便报错了也2不关联）
- 需要思考: axios请求，是否都要带https:/www.yuming.com,有没有更好的方式
- 图片有的设备有白边，因为没铺满窗口
- 超出手机屏幕，状态栏部分
- 背景永远是白色，改不成透明，改成黑色倒是可以
- 用AI来检测错误，非常高效,所以以后出问题，先说出问题，然AI排查下是否有拼写，或者传递错误问题
- 开屏广告，隐藏状态栏